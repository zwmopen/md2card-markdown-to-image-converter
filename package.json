{
  "name": "md2card-markdown-to-image-converter",
  "version": "1.1.0-lite",
  "private": true,
  "description": "MD2Card web editor, lightweight static edition, and regression tests",
  "scripts": {
    "check": "node --check script.js && node --check src/app.js && node --check src/core.js && node --check src/presets.js && node --check src/styles.js && node --check lite/core.js && node --check lite/lite.js",
    "test": "node --test tests/*.test.cjs",
    "verify": "npm run check && npm test"
  },
  "engines": { "node": ">=20" }
}
